'use strict';

( function () {
	const FORM_FIELD_SELECTOR = '[data-epdc-inquiry-products]';

	document.addEventListener( 'DOMContentLoaded', function () {
		document.querySelectorAll( FORM_FIELD_SELECTOR );
	} );
} )();
